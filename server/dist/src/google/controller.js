"use strict";
/* const passPort = require('passport')

const authGoogle = passPort.authenticate('google', { scope: ['email', 'profile'] })
const callbackGoogle = passPort.authenticate('google', { successRedirect: '/admin', failureRedirect: 'auth/failure' })

const isLoggedIn = (req: any, res: any, next: any) => {
    req.user ? next() : res.sendStatus(401)
}

const userStatus = (req: any, res: any) => {
    res.send(`Welcome administrator, ${req.user.displayName}`)
}

const authFailed = (req: any, res: any) => {
    res.send("Authentication failed")
}

module.exports = { isLoggedIn, userStatus, authGoogle, callbackGoogle, authFailed } */ 
